<li>
	{{burger_name}}

	<button class="btn btn-info btn-sm changeDevoured" data-id="{{id}}" data-devouredstate="{{devoured}}">
		{{#if devoured}}Devour Me!{{else}}Devour It Again!{{/if}}
	</button>
</li>
<br>